// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../../../utilities";

/**
 * The ordering of rules within a Ruleset dictates the sequence in which rules are evaluated for an alert source. These rules are evaluated sequentially, starting from the top.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as squadcast from "@irisdanded/pulumi-squadcast";
 * import * as squadcast from "@pulumi/squadcast";
 *
 * const exampleTeam = squadcast.getTeam({
 *     name: "Example Team",
 * });
 * const exampleUser = squadcast.getUser({
 *     email: "john@example.com",
 * });
 * const exampleService = exampleTeam.then(exampleTeam => squadcast.getService({
 *     name: "Example Service",
 *     teamId: exampleTeam.id,
 * }));
 * const exampleGer = new squadcast.Ger("exampleGer", {
 *     description: "Example GER Description",
 *     teamId: exampleTeam.then(exampleTeam => exampleTeam.id),
 *     entityOwner: {
 *         id: exampleUser.then(exampleUser => exampleUser.id),
 *         type: "user",
 *     },
 * });
 * const exampleGerRuleset = new squadcast.ger.Ruleset("exampleGerRuleset", {
 *     gerId: exampleGer.id,
 *     alertSource: "Prometheus",
 *     catchAllAction: {
 *         route_to: exampleService.then(exampleService => exampleService.id),
 *     },
 * });
 * const exampleGerRulesetRule1 = new squadcast.ger.ruleset.Rule("exampleGerRulesetRule1", {
 *     gerId: exampleGer.id,
 *     alertSource: exampleGerRuleset.alertSource,
 *     expression: "alertname == \"DeploymentReplicasNotUpdated\"",
 *     description: "Example GER Ruleset Rule",
 *     action: {
 *         route_to: exampleService.then(exampleService => exampleService.id),
 *     },
 * });
 * const exampleGerRulesetRule2 = new squadcast.ger.ruleset.Rule("exampleGerRulesetRule2", {
 *     gerId: exampleGer.id,
 *     alertSource: exampleGerRuleset.alertSource,
 *     expression: "component == \"kube-state-metrics\"",
 *     description: "Example GER Ruleset Rule",
 *     action: {
 *         route_to: exampleService.then(exampleService => exampleService.id),
 *     },
 * });
 * const ruleOrdering = new squadcast.ger.ruleset.rules.Ordering("ruleOrdering", {
 *     gerId: squadcast_ger.ger.id,
 *     alertSource: squadcast_ger_ruleset.ger_ruleset_1.alert_source,
 *     orderings: [
 *         squadcast_ger_ruleset_rule.ger_ruleset_rule_2.id,
 *         squadcast_ger_ruleset_rule.ger_ruleset_rule_1.id,
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * gerID:alertSourceName
 *
 * ```sh
 * $ pulumi import squadcast:ger/ruleset/rules/ordering:Ordering example_ger_ruleset_rule_ordering_import "53:Grafana"
 * ```
 */
export class Ordering extends pulumi.CustomResource {
    /**
     * Get an existing Ordering resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OrderingState, opts?: pulumi.CustomResourceOptions): Ordering {
        return new Ordering(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'squadcast:ger/ruleset/rules/ordering:Ordering';

    /**
     * Returns true if the given object is an instance of Ordering.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Ordering {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Ordering.__pulumiType;
    }

    /**
     * An alert source refers to the origin of an event (alert), such as a monitoring tool. These alert sources are associated with specific rules in GER, determining where events from each source should be routed. Find all alert sources supported on Squadcast [here](https://www.squadcast.com/integrations).
     */
    public readonly alertSource!: pulumi.Output<string>;
    /**
     * Shortname of the linked alert source.
     */
    public /*out*/ readonly alertSourceShortname!: pulumi.Output<string>;
    /**
     * Version of the linked alert source.
     */
    public /*out*/ readonly alertSourceVersion!: pulumi.Output<string>;
    /**
     * GER id.
     */
    public readonly gerId!: pulumi.Output<string>;
    /**
     * GER Ruleset Rule Ordering.
     */
    public readonly orderings!: pulumi.Output<string[]>;

    /**
     * Create a Ordering resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OrderingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OrderingArgs | OrderingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OrderingState | undefined;
            resourceInputs["alertSource"] = state ? state.alertSource : undefined;
            resourceInputs["alertSourceShortname"] = state ? state.alertSourceShortname : undefined;
            resourceInputs["alertSourceVersion"] = state ? state.alertSourceVersion : undefined;
            resourceInputs["gerId"] = state ? state.gerId : undefined;
            resourceInputs["orderings"] = state ? state.orderings : undefined;
        } else {
            const args = argsOrState as OrderingArgs | undefined;
            if ((!args || args.alertSource === undefined) && !opts.urn) {
                throw new Error("Missing required property 'alertSource'");
            }
            if ((!args || args.gerId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'gerId'");
            }
            if ((!args || args.orderings === undefined) && !opts.urn) {
                throw new Error("Missing required property 'orderings'");
            }
            resourceInputs["alertSource"] = args ? args.alertSource : undefined;
            resourceInputs["gerId"] = args ? args.gerId : undefined;
            resourceInputs["orderings"] = args ? args.orderings : undefined;
            resourceInputs["alertSourceShortname"] = undefined /*out*/;
            resourceInputs["alertSourceVersion"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Ordering.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Ordering resources.
 */
export interface OrderingState {
    /**
     * An alert source refers to the origin of an event (alert), such as a monitoring tool. These alert sources are associated with specific rules in GER, determining where events from each source should be routed. Find all alert sources supported on Squadcast [here](https://www.squadcast.com/integrations).
     */
    alertSource?: pulumi.Input<string>;
    /**
     * Shortname of the linked alert source.
     */
    alertSourceShortname?: pulumi.Input<string>;
    /**
     * Version of the linked alert source.
     */
    alertSourceVersion?: pulumi.Input<string>;
    /**
     * GER id.
     */
    gerId?: pulumi.Input<string>;
    /**
     * GER Ruleset Rule Ordering.
     */
    orderings?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a Ordering resource.
 */
export interface OrderingArgs {
    /**
     * An alert source refers to the origin of an event (alert), such as a monitoring tool. These alert sources are associated with specific rules in GER, determining where events from each source should be routed. Find all alert sources supported on Squadcast [here](https://www.squadcast.com/integrations).
     */
    alertSource: pulumi.Input<string>;
    /**
     * GER id.
     */
    gerId: pulumi.Input<string>;
    /**
     * GER Ruleset Rule Ordering.
     */
    orderings: pulumi.Input<pulumi.Input<string>[]>;
}
