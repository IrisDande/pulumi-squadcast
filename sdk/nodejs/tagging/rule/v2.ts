// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../../types/input";
import * as outputs from "../../types/output";
import * as utilities from "../../utilities";

/**
 * [Tagging](https://support.squadcast.com/docs/event-tagging) is a rule-based, auto-tagging system with which you can define customised tags based on incident payloads, that get automatically assigned to incidents when they are triggered.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as squadcast from "@irisdanded/pulumi-squadcast";
 * import * as squadcast from "@pulumi/squadcast";
 *
 * const exampleService = squadcast.getService({
 *     name: "example service name",
 *     teamId: data.squadcast_team.example_team.id,
 * });
 * const exampleTaggingRule = new squadcast.tagging.rule.V2("exampleTaggingRule", {
 *     serviceId: exampleService.then(exampleService => exampleService.id),
 *     isBasic: false,
 *     expression: "payload[\"event_id\"] == 40",
 *     tags: [
 *         {
 *             key: "MyTag",
 *             value: "foo",
 *             color: "#ababab",
 *         },
 *         {
 *             key: "MyTag2",
 *             value: "bar",
 *             color: "#f0f0f0",
 *         },
 *     ],
 * });
 * const exampleBasicTaggingRule = new squadcast.tagging.rule.V2("exampleBasicTaggingRule", {
 *     serviceId: exampleService.then(exampleService => exampleService.id),
 *     isBasic: true,
 *     basicExpressions: [{
 *         lhs: "payload[\"foo\"]",
 *         op: "is",
 *         rhs: "bar",
 *     }],
 *     tags: [{
 *         key: "MyTag",
 *         value: "foo",
 *         color: "#ababab",
 *     }],
 * });
 * // addTags must be set in expression when tags are not passed
 * const exampleTaggingRulesResourceWithouttags = new squadcast.tagging.rule.V2("exampleTaggingRulesResourceWithouttags", {
 *     serviceId: exampleService.then(exampleService => exampleService.id),
 *     isBasic: false,
 *     expression: "addTag(\"EventType\", payload.details.event_type_key, \"#037916\")",
 * });
 * ```
 *
 * ## Import
 *
 * serviceID:ruleID
 *
 * ```sh
 * $ pulumi import squadcast:tagging/rule/v2:V2 test 62d2fe23a57381088224d726:62da76c088f407f9ca756ca5
 * ```
 */
export class V2 extends pulumi.CustomResource {
    /**
     * Get an existing V2 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: V2State, opts?: pulumi.CustomResourceOptions): V2 {
        return new V2(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'squadcast:tagging/rule/v2:V2';

    /**
     * Returns true if the given object is an instance of V2.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is V2 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === V2.__pulumiType;
    }

    /**
     * The basic expression which needs to be evaluated to be true for this rule to apply.
     */
    public readonly basicExpressions!: pulumi.Output<outputs.tagging.rule.V2BasicExpression[] | undefined>;
    /**
     * The expression which needs to be evaluated to be true for this rule to apply.
     */
    public readonly expression!: pulumi.Output<string | undefined>;
    /**
     * is_basic will be true when users use the drop down selectors which will have lhs, op & rhs value, whereas it will be false when they use the advanced mode and it would have the expression for it's value
     */
    public readonly isBasic!: pulumi.Output<boolean>;
    /**
     * Service id.
     */
    public readonly serviceId!: pulumi.Output<string>;
    /**
     * The tags supposed to be set for a given payload(incident), Expression must be set when tags are empty and must contain addTags parameters.
     */
    public readonly tags!: pulumi.Output<outputs.tagging.rule.V2Tag[] | undefined>;

    /**
     * Create a V2 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: V2Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: V2Args | V2State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as V2State | undefined;
            resourceInputs["basicExpressions"] = state ? state.basicExpressions : undefined;
            resourceInputs["expression"] = state ? state.expression : undefined;
            resourceInputs["isBasic"] = state ? state.isBasic : undefined;
            resourceInputs["serviceId"] = state ? state.serviceId : undefined;
            resourceInputs["tags"] = state ? state.tags : undefined;
        } else {
            const args = argsOrState as V2Args | undefined;
            if ((!args || args.isBasic === undefined) && !opts.urn) {
                throw new Error("Missing required property 'isBasic'");
            }
            if ((!args || args.serviceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'serviceId'");
            }
            resourceInputs["basicExpressions"] = args ? args.basicExpressions : undefined;
            resourceInputs["expression"] = args ? args.expression : undefined;
            resourceInputs["isBasic"] = args ? args.isBasic : undefined;
            resourceInputs["serviceId"] = args ? args.serviceId : undefined;
            resourceInputs["tags"] = args ? args.tags : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(V2.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering V2 resources.
 */
export interface V2State {
    /**
     * The basic expression which needs to be evaluated to be true for this rule to apply.
     */
    basicExpressions?: pulumi.Input<pulumi.Input<inputs.tagging.rule.V2BasicExpression>[]>;
    /**
     * The expression which needs to be evaluated to be true for this rule to apply.
     */
    expression?: pulumi.Input<string>;
    /**
     * is_basic will be true when users use the drop down selectors which will have lhs, op & rhs value, whereas it will be false when they use the advanced mode and it would have the expression for it's value
     */
    isBasic?: pulumi.Input<boolean>;
    /**
     * Service id.
     */
    serviceId?: pulumi.Input<string>;
    /**
     * The tags supposed to be set for a given payload(incident), Expression must be set when tags are empty and must contain addTags parameters.
     */
    tags?: pulumi.Input<pulumi.Input<inputs.tagging.rule.V2Tag>[]>;
}

/**
 * The set of arguments for constructing a V2 resource.
 */
export interface V2Args {
    /**
     * The basic expression which needs to be evaluated to be true for this rule to apply.
     */
    basicExpressions?: pulumi.Input<pulumi.Input<inputs.tagging.rule.V2BasicExpression>[]>;
    /**
     * The expression which needs to be evaluated to be true for this rule to apply.
     */
    expression?: pulumi.Input<string>;
    /**
     * is_basic will be true when users use the drop down selectors which will have lhs, op & rhs value, whereas it will be false when they use the advanced mode and it would have the expression for it's value
     */
    isBasic: pulumi.Input<boolean>;
    /**
     * Service id.
     */
    serviceId: pulumi.Input<string>;
    /**
     * The tags supposed to be set for a given payload(incident), Expression must be set when tags are empty and must contain addTags parameters.
     */
    tags?: pulumi.Input<pulumi.Input<inputs.tagging.rule.V2Tag>[]>;
}
